# 🤖 Personal AI Multitool Assistant

Локальный CLI AI-ассистент с 8 инструментами, построенный на LangChain 0.3.x и OpenAI API.

[![Python 3.11](https://img.shields.io/badge/python-3.11-blue.svg)](https://www.python.org/downloads/)
[![LangChain](https://img.shields.io/badge/LangChain-0.3.x-green.svg)](https://www.langchain.com/)
[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)

## ✨ Возможности

- 🔍 **Поиск в интернете** — использует DuckDuckGo для поиска информации с фильтрацией по языку
- 🌤️ **Погода** — текущая погода и прогноз на завтра для любого города через OpenMeteo API
- 💰 **Криптовалюты** — актуальные курсы криптовалют через CoinGecko API
- 💱 **Валюты** — конвертация валют (включая RUB) через exchangerate API
- 📁 **Файлы** — чтение и запись файлов с проверкой безопасности
- 🎨 **QR-коды** — генерация QR-кодов из текста/URL с автоматическими именами файлов
- 💾 **Память** — сохранение важных разговоров в долговременную память (JSON)

## 📋 Требования

- **Python 3.11.x** (НЕ 3.12+ или 3.13+!)
- OpenAI API ключ
- Интернет-соединение для работы с API

## 🚀 Установка

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/ergon73/langchain-cli-multi-agent.git
cd langchain-cli-multi-agent
```

### 2. Создайте виртуальное окружение

```bash
# Windows
python -m venv .venv
.venv\Scripts\activate

# Linux/Mac
python3.11 -m venv .venv
source .venv/bin/activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Настройте переменные окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и добавьте ваш OpenAI API ключ:

```
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini
LOG_LEVEL=INFO
```

## 🎮 Использование

Запустите агента:

```bash
python run.py
```

### Примеры запросов

- `Какая погода в Москве?`
- `Найди информацию о Python программировании`
- `Какой курс биткоина?`
- `Сколько стоит 100 USD в RUB?`
- `Прочитай файл README.md`
- `Создай QR-код для https://example.com`
- `Сохрани этот разговор в память`

### Команды

- `/exit` или `/quit` — выход из программы
- `/help` — показать справку

## 📁 Структура проекта

```
langchain-cli-multi-agent/
├── agent/
│   ├── __init__.py          # Инициализация пакета
│   ├── agent.py             # Основная логика агента (LangChain)
│   └── tools.py             # Реализация всех 8 инструментов
├── .env.example             # Шаблон переменных окружения
├── .gitignore               # Правила Git
├── requirements.txt         # Зависимости Python
├── run.py                   # Точка входа (CLI интерфейс)
└── README.md                # Документация
```

**Примечание:** Файлы `agent/memory.json`, `agent.log` и папка `qr_codes/` создаются автоматически при работе и не коммитятся в репозиторий.

## 🛠️ Технологии

- **LangChain 0.3.x** — фреймворк для создания AI-агентов
- **OpenAI API** — языковая модель (GPT-4o-mini по умолчанию, можно использовать gpt-4o)
- **DuckDuckGo Search (ddgs)** — поиск в интернете с фильтрацией результатов
- **OpenMeteo API** — данные о погоде и прогнозы
- **CoinGecko API** — курсы криптовалют (бесплатный API)
- **exchangerate API** — курсы валют (поддерживает RUB и другие валюты)
- **Colorama** — цветной вывод в консоли
- **QRCode** — генерация QR-кодов

## 📝 Логирование

Все действия логируются в файл `agent.log` и выводятся в консоль. Уровень логирования можно настроить через переменную `LOG_LEVEL` в `.env`.

## 🔒 Безопасность

- API ключи хранятся в `.env` файле (не коммитится в Git)
- Файл `.env` добавлен в `.gitignore`
- Проверка путей файлов для предотвращения directory traversal
- Ограничение размера читаемых файлов (10 MB)

## 🧪 Тестирование

Проверьте работу всех инструментов:

1. **Поиск**: `Найди информацию о Python`
2. **Погода**: `Какая погода в Москве?`
3. **Криптовалюта**: `Какой курс биткоина?`
4. **Валюта**: `Сколько стоит 1 USD в RUB?`
5. **Чтение файла**: `Прочитай файл README.md`
6. **Запись файла**: `Создай файл test.txt с текстом "Привет"`
7. **QR-код**: `Создай QR-код для https://example.com`
8. **Память**: `Сохрани этот разговор в память`

## 🐛 Решение проблем

### Ошибка: "OPENAI_API_KEY not found"
- Убедитесь, что файл `.env` существует в корне проекта
- Проверьте, что ключ указан правильно

### Ошибка: "LangChain import error"
- Убедитесь, что используется Python 3.11.x
- Переустановите зависимости: `pip install -r requirements.txt`

### Ошибка: "Timeout"
- Проверьте интернет-соединение
- Некоторые API могут быть временно недоступны

## 🎯 Особенности реализации

- ✅ Поддержка 40+ городов (российские, СНГ, зарубежные)
- ✅ Умная фильтрация результатов поиска по языку
- ✅ Автоматическая генерация уникальных имен для QR-кодов
- ✅ Поддержка RUB и других валют
- ✅ Прогноз погоды на завтра
- ✅ Безопасная работа с файлами (защита от directory traversal)
- ✅ Подробное логирование всех операций

## 📊 Статус проекта

- ✅ Все 8 инструментов реализованы и протестированы
- ✅ Критические проблемы исправлены
- ✅ Готов к использованию в продакшене

## 🤝 Вклад в проект

Если вы хотите внести вклад в проект:

1. Fork репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл [LICENSE](LICENSE) для подробностей.

## 👤 Автор

**ergon73**

- GitHub: [@ergon73](https://github.com/ergon73)

Создано с использованием Cursor AI и LangChain.

## 🙏 Благодарности

- [LangChain](https://www.langchain.com/) за отличный фреймворк
- [OpenAI](https://openai.com/) за API
- Разработчикам всех используемых библиотек и API

## ⭐ Если проект был полезен

Если этот проект помог вам, поставьте звезду ⭐ на GitHub!

---

**Приятного использования!** 🚀

